// This config is not used for compilation, it is used to provide information to eslint
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
  },
  "include": [
    "packages/**/*"
  ],
  "exclude": [
    ".build",
    "dist",
    "node_modules",
    "templates"
  ],
}
